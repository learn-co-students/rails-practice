<h1><%= @author.name %> (b. <%= @author.birth_year %>)</h1>
<%= link_to "Edit #{ @author.name }", edit_author_path(@author) %>
<%= form_with model: @author, method: :delete, local: true do |form| %>
  <%= form.submit "Delete #{ @author.name }" %>
<% end %>