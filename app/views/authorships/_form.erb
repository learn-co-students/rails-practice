<%= form_with model: @authorship, url: book_authorships_path(@authorship.book.id), local: true do |form| %>
    <%= form.label :author_id %>
    <%= form.collection_select :author_id, @all_authors, :id, :name %>
    <%= form.submit "Add author" %>
<% end %>